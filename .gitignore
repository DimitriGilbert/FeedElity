# Dependencies
node_modules
.pnp
.pnp.js

# Build outputs
dist
build
*.tsbuildinfo

# Environment variables
.env
.env*.local

# Serve runtime
.serve/

# IDEs and editors
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea
*.swp
*.swo
*~
.DS_Store

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# Turbo
.turbo
.nx

# Better-T-Stack
.alchemy

# Testing
coverage
.nyc_output

# Local SQLite/libSQL runtime databases
local.db
local.db-*
local.db.backup-*
/*.db
/*.db-*
/*.sqlite
/*.sqlite-*
/*.sqlite3
/*.sqlite3-*

# Large one-off export snapshots (not source)
legacy-strapi-export.json

# Misc
*.tgz
.cache
.native
tmp
temp
.claude/settings.local.json

# Creator classification generated data (API dump + subagent shards + SQL).
# Reviewable but not source-controlled; regenerate via scripts/classify/.
scripts/classify/data/
# One-time DB repair: track source, ignore copied prod snapshots and generated reports.
scripts/db-repair/data/
scripts/db-repair/*.db
