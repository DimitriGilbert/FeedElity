.git
.native/
.turbo/
node_modules
local.db*
*.db.before-*
# Exclude the desktop app sources (Electrobun/native build, not used by the
# container images), but keep its package.json so the workspace install matches
# bun.lock and --frozen-lockfile stays valid.
apps/desktop/**
!apps/desktop/package.json
review/
.github/
*.sqlite
*.sqlite-*
*.sqlite3
*.sqlite3-*
.tanstack/
.nx/
.cache/
tmp/
temp/
coverage/
